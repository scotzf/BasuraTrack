Django>=5.0
djangorestframework>=3.15

# MySQL driver - only needed when SUKOD_DB=mysql
mysqlclient>=2.2 ; sys_platform != "win32"

# PDF rendering. Optional: /report/ falls back to print-styled HTML without it.
WeasyPrint>=61
